name: Keep Alive

on:
  workflow_dispatch:

env:
  SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: ⏸️ Workflow Disabled
        run: |
          echo "⏸️ Keep Alive workflow is temporarily disabled"
          echo "Enable it in .github/workflows/keep-alive.yml by adding the schedule trigger back"
